# linkedin-text-formatter-extension
A lightweight Chrome extension that lets users format selected LinkedIn text as bold, italic, bold italic, underline, or double underline using Unicode—without leaving the page.
